describe("Dashboard Filters Legacy", () => {
    it("moved to the smoke and regression suites", async () => {
        console.warn("Use dashboardFiltersSmokeTest.js for fast checks and dashboardFiltersRegressionTest.js for the broader matrix.");
    });
});
